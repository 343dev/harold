# Руководство по участию в разработке Harold Action

Спасибо за интерес к участию в развитии Harold Action! Мы ценим любой вклад в проект.

## Содержание

- [Кодекс поведения](#кодекс-поведения)
- [Как внести вклад](#как-внести-вклад)
- [Настройка среды разработки](#настройка-среды-разработки)
- [Процесс разработки](#процесс-разработки)
- [Стандарты кода](#стандарты-кода)
- [Тестирование](#тестирование)
- [Документация](#документация)
- [Отправка изменений](#отправка-изменений)

## Кодекс поведения

Участвуя в этом проекте, вы соглашаетесь соблюдать наш [Кодекс поведения](CODE_OF_CONDUCT.md). Пожалуйста, сообщайте о неприемлемом поведении.

## Как внести вклад

Есть много способов внести вклад в Harold Action:

### 🐛 Сообщение об ошибках

Если вы нашли ошибку:

1. Проверьте [существующие issues](https://github.com/harold-team/harold-action/issues)
2. Если похожей проблемы нет, создайте новый issue
3. Используйте шаблон для сообщения об ошибке
4. Предоставьте максимально подробную информацию

### 💡 Предложение новых функций

Для предложения новой функциональности:

1. Проверьте [roadmap проекта](https://github.com/harold-team/harold-action/projects)
2. Создайте issue с описанием предложения
3. Объясните, почему эта функция будет полезна
4. Предложите возможную реализацию

### 📝 Улучшение документации

Документация всегда может быть лучше:

- Исправление опечаток и грамматических ошибок
- Добавление примеров использования
- Улучшение существующих объяснений
- Перевод на другие языки

### 🔧 Исправление ошибок и добавление функций

Мы приветствуем pull request'ы для:

- Исправления ошибок
- Добавления новых функций
- Оптимизации производительности
- Улучшения безопасности

## Настройка среды разработки

### Требования

- **Node.js** >= 18.12.0
- **npm** >= 8.0.0
- **Git** >= 2.20.0
- **Harold CLI** (устанавливается автоматически)

### Установка

1. **Форкните репозиторий**
   ```bash
   # Замените YOUR_USERNAME на ваш GitHub username
   git clone https://github.com/YOUR_USERNAME/harold-action.git
   cd harold-action
   ```

2. **Добавьте upstream remote**
   ```bash
   git remote add upstream https://github.com/harold-team/harold-action.git
   ```

3. **Установите зависимости**
   ```bash
   cd harold-action/tests
   npm install
   ```

4. **Проверьте установку**
   ```bash
   # Запустите тесты
   npm test

   # Проверьте линтер
   npm run lint
   ```

### Структура проекта

```
harold-action/
├── action.yml              # Основной файл GitHub Action
├── README.md               # Документация пользователя
├── CHANGELOG.md            # История изменений
├── CONTRIBUTING.md         # Это руководство
├── LICENSE                 # Лицензия MIT
├── SECURITY.md             # Политика безопасности
├── scripts/                # Bash скрипты
│   ├── setup.sh           # Установка Harold
│   ├── build-and-snapshot.sh
│   ├── parallel-build.sh
│   ├── cache-manager.sh
│   └── ...
├── utils/                  # Утилиты JavaScript
│   ├── ansi-strip.js
│   └── ansi-strip.cjs
├── tests/                  # Тесты
│   ├── unit/              # Unit тесты
│   ├── integration/       # Integration тесты
│   ├── e2e/               # E2E тесты
│   └── package.json
├── examples/               # Примеры workflow
└── .github/
    └── workflows/         # CI/CD workflows
```

## Процесс разработки

### Рабочий процесс Git

1. **Создайте ветку для функции**
   ```bash
   git checkout -b feature/your-feature-name
   # или для исправления ошибки
   git checkout -b fix/bug-description
   ```

2. **Внесите изменения**
   - Следуйте [стандартам кода](#стандарты-кода)
   - Добавьте тесты для новой функциональности
   - Обновите документацию при необходимости

3. **Зафиксируйте изменения**
   ```bash
   git add .
   git commit -m "feat: add new feature description"
   ```

4. **Синхронизируйтесь с upstream**
   ```bash
   git fetch upstream
   git rebase upstream/main
   ```

5. **Отправьте изменения**
   ```bash
   git push origin feature/your-feature-name
   ```

### Соглашения о коммитах

Мы используем [Conventional Commits](https://www.conventionalcommits.org/):

```
<type>[optional scope]: <description>

[optional body]

[optional footer(s)]
```

**Типы коммитов:**
- `feat`: новая функция
- `fix`: исправление ошибки
- `docs`: изменения в документации
- `style`: форматирование кода
- `refactor`: рефакторинг без изменения функциональности
- `perf`: улучшение производительности
- `test`: добавление или изменение тестов
- `chore`: обновление зависимостей, конфигурации

**Примеры:**
```bash
feat: add caching support for build results
fix: resolve issue with ANSI code stripping
docs: update README with new configuration options
test: add unit tests for cache manager
```

## Стандарты кода

### Bash скрипты

- Используйте `#!/bin/bash` в начале файлов
- Добавляйте `set -e` для остановки при ошибках
- Используйте функции для повторяющегося кода
- Добавляйте комментарии для сложной логики
- Проверяйте входные параметры

```bash
#!/bin/bash
set -e

# Функция для вывода ошибок
error_exit() {
    echo "::error::$1" >&2
    exit 1
}

# Проверка входных параметров
if [ $# -ne 1 ]; then
    error_exit "Usage: $0 <parameter>"
fi
```

### JavaScript/Node.js

- Используйте современный JavaScript (ES2020+)
- Предпочитайте `const` и `let` вместо `var`
- Используйте async/await вместо callbacks
- Добавляйте JSDoc комментарии для функций
- Обрабатывайте ошибки явно

```javascript
/**
 * Очищает ANSI коды из текста
 * @param {string} text - Исходный текст
 * @returns {string} Очищенный текст
 */
function stripAnsiCodes(text) {
    if (typeof text !== 'string') {
        return String(text || '');
    }

    return text.replace(/\u001B\[[0-9;]*m/g, '');
}
```

### YAML файлы

- Используйте 2 пробела для отступов
- Добавляйте комментарии для сложных конфигураций
- Группируйте связанные параметры

```yaml
name: 'Harold Bundle Analyzer'
description: 'Analyze bundle size changes in Pull Requests'

inputs:
  github-token:
    description: 'GitHub token for API access'
    required: true
    default: ${{ github.token }}
```

## Тестирование

### Запуск тестов

```bash
# Все тесты
npm test

# Только unit тесты
npm run test:unit

# Только integration тесты
npm run test:integration

# Тесты с покрытием
npm run test:coverage

# Тесты в watch режиме
npm run test:watch
```

### Написание тестов

#### Unit тесты

Создавайте unit тесты для всех утилит и функций:

```javascript
describe('stripAnsiCodes', () => {
    test('should remove ANSI color codes', () => {
        const input = '\u001B[31mRed text\u001B[0m';
        const expected = 'Red text';
        expect(stripAnsiCodes(input)).toBe(expected);
    });

    test('should handle empty string', () => {
        expect(stripAnsiCodes('')).toBe('');
    });
});
```

#### Integration тесты

Тестируйте взаимодействие компонентов:

```javascript
describe('Harold Action Integration', () => {
    test('should process PR with bundle changes', async () => {
        // Настройка тестового окружения
        // Выполнение действия
        // Проверка результатов
    });
});
```

### Покрытие кода

Стремитесь к покрытию:
- **Функции**: 80%+
- **Строки**: 80%+
- **Ветки**: 70%+

## Документация

### README.md

При изменении функциональности обновляйте:
- Примеры использования
- Описания параметров
- Секцию troubleshooting

### Комментарии в коде

- Объясняйте **почему**, а не **что**
- Документируйте сложные алгоритмы
- Добавляйте ссылки на внешние ресурсы

### Примеры

Добавляйте примеры в каталог `examples/`:
- Базовое использование
- Продвинутые конфигурации
- Интеграция с другими Actions

## Отправка изменений

### Создание Pull Request

1. **Убедитесь, что все тесты проходят**
   ```bash
   npm test
   npm run lint
   ```

2. **Создайте Pull Request**
   - Используйте описательный заголовок
   - Заполните шаблон PR
   - Свяжите с соответствующими issues

3. **Шаблон описания PR**
   ```markdown
   ## Описание
   Краткое описание изменений

   ## Тип изменения
   - [ ] Исправление ошибки
   - [ ] Новая функция
   - [ ] Критическое изменение
   - [ ] Обновление документации

   ## Тестирование
   - [ ] Добавлены unit тесты
   - [ ] Добавлены integration тесты
   - [ ] Все тесты проходят

   ## Чеклист
   - [ ] Код следует стандартам проекта
   - [ ] Документация обновлена
   - [ ] CHANGELOG.md обновлен
   ```

### Процесс ревью

1. **Автоматические проверки**
   - CI/CD pipeline должен пройти успешно
   - Все тесты должны проходить
   - Покрытие кода не должно снижаться

2. **Ревью кода**
   - Минимум один approve от мейнтейнера
   - Все комментарии должны быть разрешены
   - Код должен соответствовать стандартам

3. **Мерж**
   - Используется squash merge для чистой истории
   - Commit message должен следовать соглашениям

## Релизный процесс

### Подготовка релиза

1. **Обновите версию**
   ```bash
   # В action.yml и других файлах
   ```

2. **Обновите CHANGELOG.md**
   - Переместите изменения из Unreleased в новую версию
   - Добавьте дату релиза

3. **Создайте тег**
   ```bash
   git tag -a v1.0.0 -m "Release version 1.0.0"
   git push origin v1.0.0
   ```

### Типы релизов

- **Major** (1.0.0): Критические изменения
- **Minor** (1.1.0): Новые функции
- **Patch** (1.0.1): Исправления ошибок

## Получение помощи

### Каналы связи

- **GitHub Issues**: Для багов и предложений функций
- **GitHub Discussions**: Для вопросов и обсуждений
- **Security**: security@harold-action.dev

### Ресурсы

- [Harold CLI Documentation](https://github.com/343dev/harold)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Conventional Commits](https://www.conventionalcommits.org/)

## Признание вклада

Все контрибьюторы будут добавлены в:
- Файл AUTHORS.md
- Секцию благодарностей в README.md
- Release notes для соответствующих версий

---

**Спасибо за ваш вклад в Harold Action! 🎉**
