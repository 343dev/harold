# Harold Bundle Analyzer - GitHub Marketplace

## Краткое описание

Автоматически анализируйте и отслеживайте изменения размеров бандлов в Pull Request'ах с детальными отчетами и оптимизацией производительности.

## Полное описание

Harold Bundle Analyzer - это мощный GitHub Action, который автоматически анализирует изменения размеров JavaScript бандлов в ваших Pull Request'ах. Получайте детальные отчеты о том, как ваши изменения влияют на размер приложения, и принимайте обоснованные решения о производительности.

### 🚀 Ключевые возможности

- **Автоматический анализ**: Сравнивает размеры бандлов между базовой веткой и PR
- **Детальные отчеты**: Показывает изменения по файлам, категориям и общий размер
- **Умные комментарии**: Создает и обновляет комментарии в PR с результатами анализа
- **Настраиваемые пороги**: Устанавливайте лимиты на увеличение размера бандла
- **Поддержка всех фреймворков**: React, Vue, Angular, Next.js, Svelte и другие
- **Оптимизация производительности**: Кэширование и параллельная обработка
- **Безопасность**: Специальная обработка fork репозиториев
- **Монорепозитории**: Поддержка анализа нескольких пакетов

### 📊 Что анализируется

- **Размеры файлов**: Обычные и gzip размеры
- **Категории**: JavaScript, CSS, изображения, шрифты
- **Время сборки**: Сравнение времени выполнения
- **Детализация**: Изменения по каждому файлу
- **Проценты**: Относительные изменения размеров

### 🛠️ Поддерживаемые инструменты сборки

- Webpack
- Vite
- Rollup
- Parcel
- esbuild
- Create React App
- Next.js
- Vue CLI
- Angular CLI

### 🔒 Безопасность

- Автоматическое определение fork репозиториев
- Валидация команд сборки в fork'ах
- Маскирование секретов в логах
- Ограничения размера конфигурационных файлов
- Защита от path traversal атак

### ⚡ Производительность

- Интеллектуальное кэширование снимков и сборок
- Параллельная обработка базовой и PR веток
- Early exit для PR без изменений в бандле
- Оптимизированный checkout с shallow clone

## Быстрый старт

```yaml
name: Bundle Analysis
on:
  pull_request:
    types: [opened, synchronize]

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      - run: npm ci && npm run build
      - uses: harold-team/harold-action@v1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
```

## Примеры использования

### Базовая настройка

```yaml
- uses: harold-team/harold-action@v1
  with:
    github-token: ${{ secrets.GITHUB_TOKEN }}
    build-command: 'npm run build'
    build-path: 'dist'
```

### Продвинутая конфигурация

```yaml
- uses: harold-team/harold-action@v1
  with:
    github-token: ${{ secrets.GITHUB_TOKEN }}
    build-command: 'npm run build:production'
    build-path: 'build'
    config-path: '.harold-config.js'
    fail-on-increase: 'true'
    size-threshold: '50000'  # 50KB
    percentage-threshold: '10'  # 10%
```

### Монорепозиторий

```yaml
- uses: harold-team/harold-action@v1
  with:
    github-token: ${{ secrets.GITHUB_TOKEN }}
    working-directory: 'packages/frontend'
    build-command: 'npm run build'
    comment-title: '📊 Frontend Bundle Analysis'
```

## Входные параметры

| Параметр | Описание | Обязательный | По умолчанию |
|----------|----------|--------------|--------------|
| `github-token` | GitHub token для доступа к API | Да | `${{ github.token }}` |
| `build-command` | Команда для сборки проекта | Нет | `npm run build` |
| `build-path` | Путь к директории с результатами сборки | Нет | `dist` |
| `config-path` | Путь к конфигурационному файлу harold | Нет | `.haroldrc.js` |
| `comment-title` | Заголовок комментария в PR | Нет | `📊 Bundle Size Report` |
| `fail-on-increase` | Завершить с ошибкой при увеличении размера | Нет | `false` |
| `size-threshold` | Пороговое значение увеличения в байтах | Нет | `10240` |
| `percentage-threshold` | Пороговое значение увеличения в процентах | Нет | `5` |
| `working-directory` | Рабочая директория для выполнения команд | Нет | `.` |

## Конфигурация Harold

Создайте файл `.haroldrc.js` для настройки анализа:

```javascript
export default {
  build: {
    command: 'npm run build',
    path: 'dist',
  },
  categories: {
    js: '**/*.js',
    css: '**/*.css',
    images: '**/*.{png,jpg,jpeg,gif,svg,webp}',
    fonts: '**/*.{woff,woff2,ttf,eot}',
  },
};
```

## Требования

- Node.js >= 18.12
- npm, yarn или pnpm
- Проект с командой сборки, создающей статические файлы

## Поддержка

- 📖 [Документация](https://github.com/harold-team/harold-action/blob/main/harold-action/README.md)
- 🐛 [Сообщить об ошибке](https://github.com/harold-team/harold-action/issues/new?template=bug_report.yml)
- 💡 [Предложить функцию](https://github.com/harold-team/harold-action/issues/new?template=feature_request.yml)
- 🔒 [Политика безопасности](https://github.com/harold-team/harold-action/blob/main/harold-action/SECURITY.md)

## Лицензия

MIT License - см. [LICENSE](https://github.com/harold-team/harold-action/blob/main/harold-action/LICENSE)

---

**Сделайте производительность вашего приложения видимой с Harold Bundle Analyzer!** 🚀
