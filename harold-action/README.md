# Harold Bundle Analyzer Action

GitHub Action для автоматического анализа изменений размеров бандлов в Pull Request'ах с использованием [Harold](https://github.com/343dev/harold).

## Возможности

- 🔍 Автоматический анализ изменений размеров файлов между базовой веткой и PR
- 📊 Детальные отчеты с разбивкой по категориям и файлам
- 💬 Автоматические комментарии в PR с результатами анализа
- ⚡ Кэширование для быстрой работы
- 🔧 Гибкая настройка под любой проект
- 🚨 Опциональные предупреждения при превышении пороговых значений

## Быстрый старт

Добавьте следующий workflow в `.github/workflows/bundle-analysis.yml`:

```yaml
name: Bundle Analysis
on:
  pull_request:
    types: [opened, synchronize]

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - name: Analyze bundle size
        uses: ./harold-action  # Замените на актуальный путь после публикации
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
```

## Входные параметры

| Параметр | Описание | Обязательный | По умолчанию |
|----------|----------|--------------|--------------|
| `github-token` | GitHub token для доступа к API | Да | `${{ github.token }}` |
| `build-command` | Команда для сборки проекта | Нет | `npm run build` |
| `build-path` | Путь к директории с результатами сборки | Нет | `dist` |
| `config-path` | Путь к конфигурационному файлу harold | Нет | `.haroldrc.js` |
| `comment-title` | Заголовок комментария в PR | Нет | `📊 Bundle Size Report` |
| `fail-on-increase` | Завершить с ошибкой при увеличении размера | Нет | `false` |
| `size-threshold` | Пороговое значение увеличения в байтах | Нет | `10240` |
| `percentage-threshold` | Пороговое значение увеличения в процентах | Нет | `5` |
| `working-directory` | Рабочая директория для выполнения команд | Нет | `.` |

## Примеры использования

### Базовое использование

```yaml
- name: Analyze bundle size
  uses: ./harold-action
  with:
    github-token: ${{ secrets.GITHUB_TOKEN }}
```

### Продвинутая настройка

```yaml
- name: Analyze bundle size
  uses: ./harold-action
  with:
    github-token: ${{ secrets.GITHUB_TOKEN }}
    build-command: 'npm run build:production'
    build-path: 'build'
    config-path: '.harold-config.js'
    fail-on-increase: 'true'
    size-threshold: '50000'  # 50KB
    percentage-threshold: '10'  # 10%
```

### Для монорепозитория

```yaml
- name: Analyze frontend bundle
  uses: ./harold-action
  with:
    github-token: ${{ secrets.GITHUB_TOKEN }}
    working-directory: 'packages/frontend'
    build-command: 'npm run build'
    build-path: 'dist'
```

## Требования

- Node.js >= 18.12
- npm или yarn
- Проект должен иметь команду сборки, которая создает статические файлы

## Конфигурация Harold

Action поддерживает все возможности конфигурации Harold через файл `.haroldrc.js`. Пример конфигурации:

```javascript
export default {
  build: {
    command: 'npm run build',
    path: 'dist',
  },
  categories: {
    js: '**/*.js',
    css: '**/*.css',
    images: '**/*.{png,jpg,jpeg,gif,svg,webp}',
    fonts: '**/*.{woff,woff2,ttf,eot}',
  },
};
```

## Безопасность и работа с Fork'ами

Harold Action автоматически определяет выполнение в fork репозиториях и применяет соответствующие меры безопасности.

### Автоматические проверки безопасности

- ✅ **Определение fork'ов**: Action автоматически определяет выполнение в fork репозитории
- ✅ **Валидация команд**: Проверка команд сборки на потенциально опасные операции
- ✅ **Ограничение размеров**: Лимиты на размер конфигурационных файлов (10KB)
- ✅ **Валидация параметров**: Строгая проверка всех входных параметров
- ✅ **Безопасная обработка токенов**: Автоматическое маскирование токенов в логах

### Поведение в fork'ах

При выполнении в fork репозитории Action применяет следующие ограничения:

1. **Ограниченные права комментариев**: Если у fork'а нет прав на создание комментариев, результаты выводятся в логи
2. **Валидация команд сборки**: Запрещены команды с сетевыми операциями (`curl`, `wget`, `ssh`, etc.)
3. **Проверка путей**: Запрещены попытки обхода директорий (`../`, абсолютные пути)
4. **Ограничение конфигураций**: Максимальный размер конфигурационных файлов 10KB

### Рекомендуемые настройки для fork'ов

#### Вариант 1: Стандартный `pull_request` (рекомендуется)

```yaml
on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  analyze:
    permissions:
      contents: read
      pull-requests: write  # Может быть ограничено для fork'ов
    steps:
      - uses: ./harold-action
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
```

**Плюсы**: Максимальная безопасность
**Минусы**: Комментарии могут не создаваться в fork'ах

#### Вариант 2: `pull_request_target` (для опытных пользователей)

```yaml
on:
  pull_request_target:  # ОСТОРОЖНО: Требует понимания рисков безопасности
    types: [opened, synchronize, reopened]
```

**Плюсы**: Комментарии работают в fork'ах
**Минусы**: Повышенные риски безопасности

⚠️ **Внимание**: Используйте `pull_request_target` только если понимаете риски безопасности!

📖 **Подробнее о безопасности**: См. [SECURITY.md](./SECURITY.md) для детальной информации о мерах безопасности.

### Проверка статуса безопасности

Action выводит информацию о контексте безопасности в логи:

```
🔒 Security check completed:
   Fork execution: true/false
   Write access: true/false
   Applied restrictions: fork_detected limited_write_access
```

## Troubleshooting

### Ошибка "harold command not found"

Убедитесь, что в вашем проекте есть Node.js >= 18.12. Action автоматически установит harold, но требует совместимую версию Node.js.

### Ошибка сборки проекта

Проверьте, что команда сборки (`build-command`) корректна и может быть выполнена в CI окружении. Убедитесь, что все зависимости установлены.

### Нет комментария в PR

Проверьте права доступа GitHub token. Для работы с комментариями требуются права `pull-requests: write`.

## Лицензия

MIT - см. [LICENSE](../LICENSE) файл для деталей.

## Вклад в развитие

Мы приветствуем вклад в развитие проекта! См. [CONTRIBUTING.md](CONTRIBUTING.md) для получения инструкций.
