name: 💡 Feature Request
description: Предложить новую функцию для Harold Action
title: "[Feature]: "
labels: ["enhancement", "triage"]
assignees: []

body:
  - type: markdown
    attributes:
      value: |
        Спасибо за предложение новой функции! Ваши идеи помогают сделать Harold Action лучше.

  - type: textarea
    id: problem
    attributes:
      label: Описание проблемы
      description: Какую проблему решает эта функция?
      placeholder: |
        Я всегда расстраиваюсь, когда...
        Было бы здорово, если бы...
    validations:
      required: true

  - type: textarea
    id: solution
    attributes:
      label: Предлагаемое решение
      description: Как вы видите решение этой проблемы?
      placeholder: |
        Я хотел бы, чтобы Harold Action мог...
        Это можно реализовать через...
    validations:
      required: true

  - type: textarea
    id: alternatives
    attributes:
      label: Альтернативные решения
      description: Рассматривали ли вы другие подходы?
      placeholder: |
        Альтернативный подход мог бы быть...
        Я также думал о...

  - type: dropdown
    id: feature-type
    attributes:
      label: Тип функции
      description: К какой категории относится ваше предложение?
      options:
        - Новый тип анализа
        - Улучшение производительности
        - Новый формат отчета
        - Интеграция с инструментами
        - Улучшение безопасности
        - Улучшение UX
        - Конфигурация и настройки
        - Другое
    validations:
      required: true

  - type: dropdown
    id: priority
    attributes:
      label: Приоритет
      description: Насколько важна эта функция для вас?
      options:
        - Низкий (было бы неплохо иметь)
        - Средний (улучшит мой workflow)
        - Высокий (критично для моего проекта)
        - Критический (блокирует использование Harold Action)
    validations:
      required: true

  - type: textarea
    id: use-cases
    attributes:
      label: Сценарии использования
      description: Опишите конкретные сценарии, где эта функция будет полезна
      placeholder: |
        1. Когда я работаю с монорепозиторием...
        2. При анализе библиотек...
        3. В CI/CD pipeline...
    validations:
      required: true

  - type: textarea
    id: implementation
    attributes:
      label: Предлагаемая реализация
      description: Есть ли у вас идеи о том, как это можно реализовать?
      placeholder: |
        Это можно добавить как новый параметр...
        Потребуется изменить следующие компоненты...
        Пример конфигурации:
        ```yaml
        - uses: harold-action@v1
          with:
            new-feature: true
        ```

  - type: textarea
    id: examples
    attributes:
      label: Примеры
      description: Покажите, как будет выглядеть использование новой функции
      render: yaml
      placeholder: |
        # Пример workflow с новой функцией
        - uses: harold-action@v1
          with:
            github-token: ${{ secrets.GITHUB_TOKEN }}
            new-parameter: value

  - type: textarea
    id: benefits
    attributes:
      label: Преимущества
      description: Какие преимущества принесет эта функция?
      placeholder: |
        - Ускорит анализ на 50%
        - Упростит настройку для новых пользователей
        - Добавит поддержку нового типа проектов

  - type: textarea
    id: drawbacks
    attributes:
      label: Потенциальные недостатки
      description: Есть ли потенциальные проблемы или ограничения?
      placeholder: |
        - Может увеличить время выполнения
        - Требует дополнительные зависимости
        - Усложняет конфигурацию

  - type: checkboxes
    id: compatibility
    attributes:
      label: Совместимость
      description: Как эта функция повлияет на существующих пользователей?
      options:
        - label: Функция обратно совместима
        - label: Функция требует изменений в существующих конфигурациях
        - label: Функция может нарушить существующие workflow

  - type: textarea
    id: additional-context
    attributes:
      label: Дополнительный контекст
      description: Любая другая информация о предложении
      placeholder: |
        - Ссылки на похожие функции в других инструментах
        - Исследования пользователей
        - Статистика использования
        - Скриншоты или mockup'ы

  - type: checkboxes
    id: contribution
    attributes:
      label: Готовность к участию
      description: Готовы ли вы помочь с реализацией?
      options:
        - label: Я готов помочь с дизайном функции
        - label: Я готов помочь с реализацией
        - label: Я готов помочь с тестированием
        - label: Я готов помочь с документацией

  - type: checkboxes
    id: terms
    attributes:
      label: Подтверждение
      description: Пожалуйста, подтвердите следующее
      options:
        - label: Я проверил существующие issues и feature request'ы
          required: true
        - label: Я проверил roadmap проекта
          required: true
        - label: Эта функция будет полезна не только мне
          required: true
